'use strict';

define([
    'react'
], function (React) {
	var InboxComponent = React.createClass({
        render : function() {
            return (<div>Inbox</div>);
        }
    });

    return InboxComponent;
});
