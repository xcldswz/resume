'use strict';

define([
    'react'
], function (React) {
	var IndexComponent = React.createClass({
        render : function() {
            return (<div>Hello, World</div>);
        }
    });

    return IndexComponent;
});
