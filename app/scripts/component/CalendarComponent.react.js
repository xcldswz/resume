'use strict';

define([
    'react'
],function(React){
    var CalendarComponent = React.createClass({
        render : function() {
            return (<div>Calendar</div>);
        }
    });

    return CalendarComponent;
});
